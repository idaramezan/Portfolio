import { useEffect, useMemo, useState } from "react";
import { Link, useLocation } from "wouter";
import { Check, Copy, Upload } from "lucide-react";
import { clearCart, loadCart, type CartItem, type ShoppingRegion } from "@/lib/store";
import { useLocale } from "@/lib/locale";
import { trackAnalytics } from "@/lib/analytics";

const PROVINCES = "Adana,Adıyaman,Afyonkarahisar,Ağrı,Aksaray,Amasya,Ankara,Antalya,Ardahan,Artvin,Aydın,Balıkesir,Bartın,Batman,Bayburt,Bilecik,Bingöl,Bitlis,Bolu,Burdur,Bursa,Çanakkale,Çankırı,Çorum,Denizli,Diyarbakır,Düzce,Edirne,Elazığ,Erzincan,Erzurum,Eskişehir,Gaziantep,Giresun,Gümüşhane,Hakkâri,Hatay,Iğdır,Isparta,İstanbul,İzmir,Kahramanmaraş,Karabük,Karaman,Kars,Kastamonu,Kayseri,Kilis,Kırıkkale,Kırklareli,Kırşehir,Kocaeli,Konya,Kütahya,Malatya,Manisa,Mardin,Mersin,Muğla,Muş,Nevşehir,Niğde,Ordu,Osmaniye,Rize,Sakarya,Samsun,Şanlıurfa,Siirt,Sinop,Sivas,Şırnak,Tekirdağ,Tokat,Trabzon,Tunceli,Uşak,Van,Yalova,Yozgat,Zonguldak".split(",");
const COUNTRY_CODES = "AD AE AF AG AI AL AM AO AR AT AU AZ BA BB BD BE BF BG BH BI BJ BN BO BR BS BT BW BY BZ CA CD CF CG CH CI CL CM CN CO CR CU CV CY CZ DE DJ DK DM DO DZ EC EE EG ER ES ET FI FJ FM FR GA GB GD GE GH GM GN GQ GR GT GW GY HK HN HR HT HU ID IE IL IN IQ IR IS IT JM JO JP KE KG KH KI KM KN KP KR KW KZ LA LB LC LI LK LR LS LT LU LV LY MA MC MD ME MG MH MK ML MM MN MR MT MU MV MW MX MY MZ NA NE NG NI NL NO NP NR NZ OM PA PE PG PH PK PL PS PT PW PY QA RO RS RU RW SA SB SC SD SE SG SI SK SL SM SN SO SR SS ST SV SY SZ TD TG TH TJ TL TM TN TO TR TT TV TW TZ UA UG UY UZ VA VC VE VN VU WS YE ZA ZM ZW".split(" ");
type Quote = { market: string; currency: "TRY" | "USD"; items: Array<{name:string;quantity:number;lineTotalMinor:number}>; subtotalMinor:number; shippingMinor:number; grandTotalMinor:number; printQuantity:number; originalQuantity:number };
type Bank = { account_holder:string; bank_name:string; iban:string; swift_bic?:string; branch_info?:string; instructions?:string };
const initial = { fullName:"",email:"",phone:"",countryCode:"TR",countryName:"Türkiye",province:"",district:"",city:"",postalCode:"",address:"",deliveryNotes:"",consent:false,honey:"" };
function productId(item: CartItem) { const base=item.id.split(":")[0]; return item.productId || base.replace(/^original-/,"").replace(/^print-product-/,"").replace(/^product-/,""); }
function itemsFor(cart: CartItem[]) { return cart.map(item=>({productId:productId(item),kind:item.kind === "original" ? "original" : item.kind === "print" || item.kind === "product" ? "print" : item.kind,quantity:item.quantity,selectedOptions:item.printConfiguration?{sizeId:item.printConfiguration.sizeId,framing:item.printConfiguration.framing,color:item.selectedColor}: {color:item.selectedColor}})); }
const money=(minor:number,currency:string)=>new Intl.NumberFormat(currency==="TRY"?"tr-TR":"en-US",{style:"currency",currency}).format(minor/100);

export default function Checkout({ market }: { market: "turkiye" | "international_original" }) {
  const region: ShoppingRegion = market === "turkiye" ? "TR" : "INTERNATIONAL";
  const { locale } = useLocale(); const [,navigate]=useLocation();
  const cart=useMemo(()=>loadCart(region),[region]); const key=`checkout-draft:${market}`;
  const [form,setForm]=useState<typeof initial>(()=>{const base={...initial,countryCode:market==="turkiye"?"TR":"",countryName:market==="turkiye"?"Türkiye":""};try{return {...base,...JSON.parse(sessionStorage.getItem(key)||"{}"),consent:false};}catch{return base;}});
  const [quote,setQuote]=useState<Quote|null>(null),[bank,setBank]=useState<Bank|null>(null),[receipt,setReceipt]=useState<File|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState(""),[copied,setCopied]=useState("");
  const idempotency=useMemo(()=>{const k=`checkout-idempotency:${market}`;let value=sessionStorage.getItem(k);if(!value){value=crypto.randomUUID();sessionStorage.setItem(k,value);}return value;},[market]);
  const reference=`PAY-${idempotency.slice(0,8).toUpperCase()}`;
  const countries=useMemo(()=>{const names=new Intl.DisplayNames([locale],{type:"region"});return COUNTRY_CODES.map(code=>({code,name:names.of(code)||code})).sort((a,b)=>a.name.localeCompare(b.name,locale));},[locale]);
  useEffect(()=>{sessionStorage.setItem(key,JSON.stringify({...form,consent:false}));},[form,key]);
  useEffect(()=>{trackAnalytics("checkout_started",{metadata:{market,productCount:cart.length}});fetch("/api/checkout/quote",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({market,items:itemsFor(cart)})}).then(async r=>{const p=await r.json();if(!r.ok)throw new Error(p.error);setQuote(p);return fetch(`/api/checkout/bank/${p.currency}`)}).then(async r=>{const p=await r.json();if(!r.ok)throw new Error(p.error);setBank(p);trackAnalytics("bank_instructions_viewed",{metadata:{market}});}).catch(e=>setError(e.message));},[market]);
  const set=(name:string,value:any)=>setForm(current=>({...current,[name]:value}));
  const copy=async(label:string,value:string)=>{await navigator.clipboard.writeText(value);setCopied(`${label} copied`);setTimeout(()=>setCopied(""),1800);};
  const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!receipt||!quote||!bank)return setError("Upload your completed bank-transfer receipt before submitting.");setBusy(true);setError("");try{const payload={...form,market,language:locale,idempotencyKey:idempotency,items:itemsFor(cart),phone:market==="turkiye"?`+90${form.phone.replace(/\D/g,"").replace(/^0/,"")}`:form.phone.trim(),city:market==="turkiye"?form.district:form.city};const data=new FormData();data.append("payload",JSON.stringify(payload));data.append("receipt",receipt);trackAnalytics("receipt_upload_started",{metadata:{market}});const response=await fetch("/api/checkout/orders",{method:"POST",body:data});const result=await response.json();if(!response.ok)throw new Error(result.error);trackAnalytics("order_submitted",{metadata:{market,currency:quote.currency,productCount:quote.items.length}});clearCart(region);sessionStorage.removeItem(key);sessionStorage.removeItem(`checkout-idempotency:${market}`);navigate(`/checkout/success/${result.orderNumber}`);}catch(reason){setError(reason instanceof Error?reason.message:"Order could not be submitted.");}finally{setBusy(false);}};
  if(!cart.length&&!quote)return <main className="checkout-page"><div className="checkout-empty"><h1>Your basket is empty</h1><Link href={market==="turkiye"?"/shop/turkiye":"/shop/international"} className="button-primary">Return to the shop</Link></div></main>;
  return <main className="checkout-page"><div className="checkout-progress" aria-label="Checkout progress"><span>1. Basket</span><strong>2. Delivery</strong><strong>3. Payment</strong><span>4. Complete</span></div><div className="checkout-layout"><form onSubmit={submit} className="checkout-form" noValidate><header><p className="eyebrow">Bank-transfer checkout</p><h1>{market==="turkiye"?"Complete your Türkiye order":"International original order"}</h1><p>Please make sure your information is complete and accurate. Aida will use it to prepare and deliver your order.</p></header>{error&&<div role="alert" tabIndex={-1} className="checkout-error">{error}</div>}<section className="checkout-panel"><h2>Contact details</h2><div className="checkout-fields"><Field label="Full name"><input required autoComplete="name" value={form.fullName} onChange={e=>set("fullName",e.target.value)}/></Field><Field label="Email address"><input required type="email" autoComplete="email" value={form.email} onChange={e=>set("email",e.target.value)}/></Field><Field label={market==="turkiye"?"Phone (+90)":"International phone number"}><input required type="tel" autoComplete="tel" placeholder={market==="turkiye"?"5xxxxxxxxx":"+44…"} value={form.phone} onChange={e=>set("phone",e.target.value)}/></Field></div></section><section className="checkout-panel"><h2>Delivery address</h2><div className="checkout-fields">{market==="turkiye"?<><Field label="Country"><input value="Türkiye" readOnly/></Field><Field label="Province"><select required autoComplete="address-level1" value={form.province} onChange={e=>set("province",e.target.value)}><option value="">Choose a province</option>{PROVINCES.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="District"><input required autoComplete="address-level2" value={form.district} onChange={e=>set("district",e.target.value)}/></Field></>:<><Field label="Country"><select required autoComplete="country-name" value={form.countryCode} onChange={e=>{const found=countries.find(x=>x.code===e.target.value);setForm(c=>({...c,countryCode:e.target.value,countryName:found?.name||""}));}}><option value="">Choose a country</option>{countries.map(x=><option key={x.code} value={x.code}>{x.name}</option>)}</select></Field><Field label="City"><input required autoComplete="address-level2" value={form.city} onChange={e=>set("city",e.target.value)}/></Field><Field label="State, province or region"><input required autoComplete="address-level1" value={form.province} onChange={e=>set("province",e.target.value)}/></Field></>}<Field label={market==="turkiye"?"Postal code (5 digits)":"Postal or ZIP code"}><input required autoComplete="postal-code" value={form.postalCode} onChange={e=>set("postalCode",e.target.value)}/></Field><div className="checkout-field--wide"><Field label="Full delivery address"><textarea required rows={5} autoComplete="street-address" value={form.address} onChange={e=>set("address",e.target.value)} placeholder="Neighbourhood, street, building and apartment number"/></Field></div><div className="checkout-field--wide"><Field label="Delivery instructions (optional)"><textarea rows={3} value={form.deliveryNotes} onChange={e=>set("deliveryNotes",e.target.value)}/></Field></div></div></section>{quote&&bank&&<section className="checkout-panel checkout-bank"><p className="eyebrow">Payment</p><h2>Complete your bank transfer</h2><p>Please transfer the exact total before submitting your order. Use the reference where possible.</p><CopyRow label="Amount to transfer" value={money(quote.grandTotalMinor,quote.currency)} onCopy={copy}/><CopyRow label="Account holder" value={bank.account_holder}/><CopyRow label="Bank" value={bank.bank_name}/><CopyRow label="IBAN" value={bank.iban} onCopy={copy}/>{bank.swift_bic&&<CopyRow label="SWIFT / BIC" value={bank.swift_bic}/>}<CopyRow label="Payment reference" value={reference} onCopy={copy}/>{bank.instructions&&<p className="checkout-bank__note">{bank.instructions}</p>}</section>}<section className="checkout-panel"><h2>Payment receipt</h2><p>Upload your completed bank-transfer receipt. Accepted formats: JPG, PNG, WebP or PDF. Maximum 10 MB.</p><label className="receipt-upload"><Upload aria-hidden="true"/><span>{receipt?`${receipt.name} · ${(receipt.size/1024/1024).toFixed(2)} MB`:"Choose receipt or take a photo"}</span><input required type="file" accept="image/jpeg,image/png,image/webp,application/pdf" capture="environment" onChange={e=>{setReceipt(e.target.files?.[0]||null);trackAnalytics("receipt_upload_completed",{metadata:{market}});}}/></label>{receipt&&<button type="button" className="button-link" onClick={()=>setReceipt(null)}>Remove file</button>}<label className="checkout-consent"><input type="checkbox" checked={form.consent} onChange={e=>set("consent",e.target.checked)} required/><span>I confirm that my delivery and contact information is accurate, and I agree that it may be used to process and deliver this order. <Link href="/privacy">Privacy policy</Link></span></label><input className="sr-only" tabIndex={-1} autoComplete="off" value={form.honey} onChange={e=>set("honey",e.target.value)}/><button disabled={busy||!quote||!bank} className="button-primary checkout-submit">{busy?"Submitting…":market==="turkiye"?"Submit order for payment review":"Submit international original order"}</button></section></form><aside className="checkout-summary"><p className="eyebrow">Order summary</p><h2>Review your basket</h2>{quote?<><ul>{quote.items.map((item,index)=><li key={index}><span>{item.name} × {item.quantity}</span><strong>{money(item.lineTotalMinor,quote.currency)}</strong></li>)}</ul><dl><div><dt>Products</dt><dd>{money(quote.subtotalMinor,quote.currency)}</dd></div><div><dt>Shipping</dt><dd>{money(quote.shippingMinor,quote.currency)}</dd></div><div className="checkout-total"><dt>Total</dt><dd>{money(quote.grandTotalMinor,quote.currency)}</dd></div></dl><p className="checkout-shipping-copy">{market==="turkiye"?(quote.printQuantity?`Delivery calculation: 200 TL for the first print${quote.printQuantity>1?` + 20 TL for ${quote.printQuantity-1} additional print${quote.printQuantity>2?"s":""}`:""}. Originals ship free.`:"Original paintings ship free within Türkiye."):"Fixed international original shipping: 100 USD per order."}</p></>:<p>Calculating authoritative prices…</p>}</aside></div><p className="sr-only" role="status" aria-live="polite">{copied}</p></main>;
}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="checkout-field"><span>{label}</span>{children}</label>}
function CopyRow({label,value,onCopy}:{label:string;value:string;onCopy?:(label:string,value:string)=>void}){return <div className="checkout-copy-row"><span><small>{label}</small><strong>{value}</strong></span>{onCopy&&<button type="button" onClick={()=>onCopy(label,value)} aria-label={`Copy ${label}`}><Copy/></button>}</div>}

export function CheckoutSuccess({ orderNumber }: { orderNumber: string }) { const [order,setOrder]=useState<any>(null);useEffect(()=>{fetch(`/api/checkout/orders/${encodeURIComponent(orderNumber)}`).then(r=>r.json()).then(setOrder);},[orderNumber]);return <main className="checkout-page"><section className="checkout-success"><span className="checkout-success__icon"><Check/></span><p className="eyebrow">Pending payment review</p><h1>Your order has been received</h1><p>Order <strong>{orderNumber}</strong> is awaiting payment verification.</p>{order&&<><p>A confirmation has been sent to {order.customer_email}.</p><p className="checkout-success__total">{money(order.grand_total_minor,order.currency)}</p></>}<p>Aida will review the transfer receipt and begin preparing your order after the payment is confirmed.</p><Link href="/" className="button-primary">Return to the studio</Link></section></main> }
